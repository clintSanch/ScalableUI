import { MainDirective } from './main.directive';

describe('MainDirective', () => {
  it('should create an instance', () => {
    const directive = new MainDirective();
    expect(directive).toBeTruthy();
  });
});
